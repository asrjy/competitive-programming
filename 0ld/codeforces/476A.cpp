#include<iostream>
using namespace std;
int main(){
    int s(0), m(0);
    cin >> s >> m;
    int temp(0), a(0);
    if(m > 3) {
        cout << "-1" << endl;
        return 0;
    } else if()

    return 0;
}