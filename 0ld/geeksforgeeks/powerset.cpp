#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t(0);
    cin >> t;
    while (t--)
    {
        string mstr;
        vector<string> outputstr;
        cin >> mstr;
        int strleng(mstr.length()), iter(0);
    }
}